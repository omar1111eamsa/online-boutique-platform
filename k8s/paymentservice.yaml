apiVersion: apps/v1
kind: Deployment
metadata:
  name: paymentservice
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paymentservice
  template:
    metadata:
      labels:
        app: paymentservice
    spec:
      containers:
        - name: paymentservice
          image: 858093957996.dkr.ecr.eu-west-1.amazonaws.com/paymentservice:v1
          ports:
            - containerPort: 50051
          env:
            - name: PORT
              value: "50051"
            - name: DISABLE_PROFILER
              value: "true"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          readinessProbe:
            grpc:
              port: 50051
          livenessProbe:
            grpc:
              port: 50051
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: paymentservice
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: paymentservice
  ports:
    - port: 50051
      targetPort: 50051
