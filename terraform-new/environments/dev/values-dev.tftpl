global:
  awsAccountId: "${aws_account_id}"
  awsRegion: "${aws_region}"
  clusterName: "${cluster_name}"
  karpenterNodeRoleName: "${cluster_name}-karpenter-node-role"
  ingress:
    host: "${domain_name}"
    certificateArn: "${certificate_arn}"